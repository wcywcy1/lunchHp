# 多组织安全加固 — 代码审查与修复计划

> 版本：v3.0 | 2026-06-25
> 前置：已完成的 groupId 隔离修复（commit 6df8e75）
> 兼容约束：不影响已上线版本（git: a45713c0）的运行

---

## 一、背景与问题

小程序最初为单组织（`lunch_hp`）设计，后扩展为多组织。审查发现以下安全漏洞：

1. **`getMemberByOpenid` 的 `recovered` 后门** — 组不存在时自动返回 creator 身份，可伪造身份
2. **`getInitData` 自动加入** — 任何人可自动加入任意组
3. **`joinGroup`/`joinGroupByName` 无审批** — 任何人可自由加入
4. **`_autoCancelExpiredPending` 缺 groupId** — 纵深防御缺失
5. **`getDataTimestamps` 无身份校验** — 任何人可读任何组的通知
6. **`parseXlsx`/`parseCsv` 无身份校验** — 可被利用下载云存储文件

---

## 二、修复项总览

| # | 优先级 | 修复项 | 文件 | 兼容策略 |
|---|--------|--------|------|---------|
| 1 | 🔴高 | `getMemberByOpenid` recovered 后门限制 | lunch_menu, lunch_order | 仅 `lunch_hp` 允许 |
| 2 | 🔴高 | `getInitData` 自动加入限制 | lunch_order | 仅 `lunch_hp` 自动加入 |
| 3 | 🔴高 | `joinGroup`/`joinGroupByName` 加入审批 | lunch_menu | `lunch_hp` 自动通过，其他组 pending |
| 4 | 🔴高 | 新增审批流程 action | lunch_menu | 新增，不影响旧逻辑 |
| 5 | 🟡中 | `checkRole` 拒绝 pending 成员 | lunch_menu, lunch_order | 新增检查，旧成员无 status 字段不受影响 |
| 6 | 🟡中 | `_autoCancelExpiredPending` 加 groupId | lunch_order | 纯加过滤条件 |
| 7 | 🟡中 | `getDataTimestamps` 加身份校验 | lunch_menu | 正常用户都是成员 |
| 8 | 🟢低 | `parseXlsx`/`parseCsv` 加身份校验 | lunch_menu | 正常用户都是成员 |
| 9 | 🟢低 | 标记废弃代码 | lunch_menu, lunch_order | 仅加注释 |

---

## 三、逐项详细设计

### 修复 1：`getMemberByOpenid` — recovered 后门限制

**文件**：`wxcloud/functions/lunch_menu/index.js` L59-68, `wxcloud/functions/lunch_order/index.js` L50-59

**当前代码**：
```js
async function getMemberByOpenid(openid) {
    const { data } = await db.collection(COL.MEMBERS)
        .where({ groupId: GROUP_ID, openid }).get()
    if (data[0]) return data[0]
    const groupData = (await db.collection(COL.GROUPS).doc(GROUP_ID).get()).data
    if (groupData && groupData.creatorId === openid) {
        return { _id: 'recovered', groupId: GROUP_ID, openid, role: ROLE.CREATOR, name: 'creator' }
    }
    return null
}
```

**修复后**：
```js
async function getMemberByOpenid(openid) {
    const { data } = await db.collection(COL.MEMBERS)
        .where({ groupId: GROUP_ID, openid, status: _.neq('rejected') }).get()
    if (data[0]) return data[0]
    // [DEPRECATED] recovered 后门仅为兼容旧版客户端(a45713c0)，新版本上线后删除此块
    if (GROUP_ID === 'lunch_hp') {
        const groupData = (await db.collection(COL.GROUPS).doc(GROUP_ID).get()).data
        if (groupData && groupData.creatorId === openid) {
            return { _id: 'recovered', groupId: GROUP_ID, openid, role: ROLE.CREATOR, name: 'creator' }
        }
    }
    return null
}
```

**变更点**：
- recovered 后门仅对 `lunch_hp` 生效，其他组不再有后门
- 查询排除 `status: 'rejected'` 的成员
- 旧成员无 `status` 字段，`_.neq('rejected')` 不影响

**新版本上线后可删除**：
- 整个 `if (GROUP_ID === 'lunch_hp')` 块
- `_id: 'recovered'` 相关的所有处理逻辑

---

### 修复 2：`getInitData` — 自动加入限制

**文件**：`wxcloud/functions/lunch_order/index.js` L195-230

**当前代码**：当用户不是成员时，自动创建成员记录并返回。

**修复后**：
```js
if (creatorMatch) {
    // [DEPRECATED] recovered 后门仅为兼容旧版客户端(a45713c0)，新版本上线后删除此块
    if (GROUP_ID === 'lunch_hp') {
        currentMember = { _id: 'recovered', groupId: GROUP_ID, openid, role: ROLE.CREATOR, name: 'creator' }
        memberRole = ROLE.CREATOR
    }
} else if (openid) {
    // [DEPRECATED] 以下自动加入逻辑仅为兼容旧版客户端(a45713c0)，新版本上线后删除此块
    if (GROUP_ID === 'lunch_hp') {
        const now2 = db.serverDate()
        const newMember = {
            groupId: GROUP_ID, openid, name: '', nickName: '', avatar: '',
            role: ROLE.MEMBER, isVirtual: false, privacyAgreed: false, joinedAt: now2,
        }
        const addRes = await db.collection(COL.MEMBERS).add({ data: newMember })
        currentMember = { ...newMember, _id: addRes._id }
        membersResult.data.push(currentMember)
        isNew = true
        await _touchMenuAndMembersTimestamp()
    }
    // 新版行为：非成员返回 null，前端引导加入/创建
}
```

**新版本上线后可删除**：
- `if (GROUP_ID === 'lunch_hp')` 中的自动加入逻辑
- `creatorMatch` 中的 recovered 逻辑

---

### 修复 3：`joinGroup` / `joinGroupByName` — 加入审批

**文件**：`wxcloud/functions/lunch_menu/index.js`

**`joinGroupByName` 修复后**：
```js
// [DEPRECATED] lunch_hp 自动通过仅为兼容旧版客户端(a45713c0)，新版本上线后删除
const autoApprove = targetGroupId === 'lunch_hp'

const member = {
    groupId: targetGroupId,
    name: '', nickName: '', avatar: '', openid,
    role: ROLE.MEMBER, isVirtual: false, privacyAgreed: false,
    status: autoApprove ? 'active' : 'pending',
    joinedAt: now,
}
```

**`joinGroup` 修复后**：
```js
// [DEPRECATED] lunch_hp 自动通过仅为兼容旧版客户端(a45713c0)，新版本上线后删除
const autoApprove = GROUP_ID === 'lunch_hp'

const member = {
    groupId: GROUP_ID,
    name: name || '', nickName: nickName || '', avatar: '', openid,
    role: ROLE.MEMBER, isVirtual: false, privacyAgreed: false,
    status: autoApprove ? 'active' : 'pending',
    joinedAt: now,
}
```

**新版本上线后可删除**：
- `autoApprove` 变量及 `lunch_hp` 判断
- 所有 `status: autoApprove ? 'active' : 'pending'` 改为 `status: 'pending'`

---

### 修复 4：新增审批流程 action

**文件**：`wxcloud/functions/lunch_menu/index.js`

新增 3 个函数：

```js
async function getPendingMembers(event, openid) {
    const caller = await getMemberByOpenid(openid)
    if (!checkRole(caller, ROLE.ADMIN, ROLE.CREATOR)) return { code: 403, msg: 'admin/creator only' }
    const { data } = await db.collection(COL.MEMBERS)
        .where({ groupId: GROUP_ID, status: 'pending' })
        .orderBy('joinedAt', 'asc')
        .get()
    return { code: 0, data }
}

async function approveJoin(event, openid) {
    const { memberId } = event
    if (!memberId) return { code: 400, msg: 'missing memberId' }
    const caller = await getMemberByOpenid(openid)
    if (!checkRole(caller, ROLE.ADMIN, ROLE.CREATOR)) return { code: 403, msg: 'admin/creator only' }
    const target = (await db.collection(COL.MEMBERS).doc(memberId).get()).data
    if (!target) return { code: 404, msg: 'member not found' }
    if (target.groupId && target.groupId !== GROUP_ID) return { code: 403, msg: 'not in current group' }
    if (target.status !== 'pending') return { code: 400, msg: 'member is not pending' }
    await db.collection(COL.MEMBERS).doc(memberId).update({ data: { status: 'active' } })
    await updateGroupTimestamp('membersTimestamp')
    return { code: 0 }
}

async function rejectJoin(event, openid) {
    const { memberId } = event
    if (!memberId) return { code: 400, msg: 'missing memberId' }
    const caller = await getMemberByOpenid(openid)
    if (!checkRole(caller, ROLE.ADMIN, ROLE.CREATOR)) return { code: 403, msg: 'admin/creator only' }
    const target = (await db.collection(COL.MEMBERS).doc(memberId).get()).data
    if (!target) return { code: 404, msg: 'member not found' }
    if (target.groupId && target.groupId !== GROUP_ID) return { code: 403, msg: 'not in current group' }
    if (target.status !== 'pending') return { code: 400, msg: 'member is not pending' }
    await db.collection(COL.MEMBERS).doc(memberId).remove()
    return { code: 0 }
}
```

注册到 handlers：
```js
const handlers = {
    // ...existing...
    getPendingMembers,
    approveJoin,
    rejectJoin,
}
```

**新版本上线后**：这些函数保留，是正式功能。

---

### 修复 5：`checkRole` 拒绝 pending 成员

**文件**：`wxcloud/functions/lunch_menu/index.js` L70-73, `wxcloud/functions/lunch_order/index.js` L61-64

**当前代码**：
```js
function checkRole(member, ...allowed) {
    if (!member) return false
    return allowed.includes(member.role)
}
```

**修复后**：
```js
function checkRole(member, ...allowed) {
    if (!member) return false
    if (member.status === 'pending') return false
    return allowed.includes(member.role)
}
```

**说明**：旧成员无 `status` 字段，`member.status` 为 `undefined`，不等于 `'pending'`，不受影响。

---

### 修复 6：`_autoCancelExpiredPending` 加 groupId

**文件**：`wxcloud/functions/lunch_order/index.js` L496-498

**当前代码**：
```js
await db.collection(COL.ORDERS)
    .where({ _id: _.in(chunk.map(o => o._id)) })
    .update({ data: { status: STATUS.CANCELLED, updatedAt: now2 } })
```

**修复后**：
```js
await db.collection(COL.ORDERS)
    .where({ _id: _.in(chunk.map(o => o._id)), groupId: GROUP_ID })
    .update({ data: { status: STATUS.CANCELLED, updatedAt: now2 } })
```

---

### 修复 7：`getDataTimestamps` 加身份校验

**文件**：`wxcloud/functions/lunch_menu/index.js` L145-156

**当前代码**：
```js
async function getDataTimestamps(event, openid) {
    const { data } = await db.collection(COL.GROUPS).doc(GROUP_ID).get().catch(() => ({ data: {} }))
    return { code: 0, data: { ... } }
}
```

**修复后**：
```js
async function getDataTimestamps(event, openid) {
    const caller = await getMemberByOpenid(openid)
    if (!caller) return { code: 403, msg: 'member only' }
    const { data } = await db.collection(COL.GROUPS).doc(GROUP_ID).get().catch(() => ({ data: {} }))
    return { code: 0, data: { ... } }
}
```

---

### 修复 8：`parseXlsx`/`parseCsv` 加身份校验

**文件**：`wxcloud/functions/lunch_menu/index.js`

**`parseXlsx` 修复后**：
```js
async function parseXlsx(event, openid) {
    const caller = await getMemberByOpenid(openid)
    if (!caller) return { code: 403, msg: 'member only' }
    // ...existing code...
}
```

**`parseCsv` 修复后**：
```js
async function parseCsv(event, openid) {
    const caller = await getMemberByOpenid(openid)
    if (!caller) return { code: 403, msg: 'member only' }
    // ...existing code...
}
```

---

### 修复 9：标记废弃代码

在以下位置加 `[DEPRECATED]` 注释：

| 文件 | 位置 | 注释内容 |
|------|------|---------|
| lunch_menu L92 | `GROUP_ID = event.groupId \|\| 'lunch_hp'` | `[DEPRECATED] 默认值 'lunch_hp' 仅为兼容旧版客户端(a45713c0)，新版本上线后改为必传` |
| lunch_order L104 | `GROUP_ID = event.groupId \|\| 'lunch_hp'` | 同上 |
| lunch_menu L175-190 | `initGroup` 函数 | `[DEPRECATED] initGroup 仅为兼容旧版客户端(a45713c0)，新版本上线后删除此函数` |

---

## 四、新版本上线后可删除清单

以下代码仅为兼容旧版客户端（a45713c0），新版本上线后可安全删除：

### 云函数 `lunch_menu`

| 位置 | 代码 | 说明 |
|------|------|------|
| L92 | `event.groupId \|\| 'lunch_hp'` | 改为 `event.groupId`（必传） |
| L63-66 | `if (groupData && groupData.creatorId === openid) { return { _id: 'recovered', ... } }` | recovered 后门 |
| L175-190 | `async function initGroup(...)` | 整个函数 |
| L311 | `existing && existing._id !== 'recovered'` | recovered 判断 |
| L338-340 | `if (groupData && groupData.creatorId === openid) { ... role: ROLE.CREATOR }` | joinGroup 中 creator 自动提升 |
| joinGroupByName | `const autoApprove = targetGroupId === 'lunch_hp'` | lunch_hp 自动通过 |
| joinGroup | `const autoApprove = GROUP_ID === 'lunch_hp'` | lunch_hp 自动通过 |

### 云函数 `lunch_order`

| 位置 | 代码 | 说明 |
|------|------|------|
| L104 | `event.groupId \|\| 'lunch_hp'` | 改为 `event.groupId`（必传） |
| L54-56 | `if (groupData && groupData.creatorId === openid) { return { _id: 'recovered', ... } }` | recovered 后门 |
| L195-230 | getInitData 中的自动加入逻辑 | 非成员应返回 null |
| L211-212 | `currentMember = { _id: 'recovered', ... }` | recovered 身份 |

### 云函数 `lunch_backup`

| 位置 | 代码 | 说明 |
|------|------|------|
| L6 | `event.groupId \|\| 'lunch_hp'` | 改为 `event.groupId`（必传） |

### 前端

| 文件 | 代码 | 说明 |
|------|------|------|
| appInit.ts | `menuAction('initGroup')` | 整个 initGroup 调用可删除 |
| appConfig.ts | `export const GROUP_ID = 'lunch_hp'` | 改为动态获取，不再硬编码 |

---

## 五、执行顺序

每项修复后：语法检查 → commit → push

1. ✅ 修复 1：`getMemberByOpenid` recovered 后门限制
2. ✅ 修复 2：`getInitData` 自动加入限制
3. ✅ 修复 3：`joinGroup`/`joinGroupByName` 加入审批
4. ✅ 修复 4：新增审批流程 action
5. ✅ 修复 5：`checkRole` 拒绝 pending 成员
6. ✅ 修复 6：`_autoCancelExpiredPending` 加 groupId
7. ✅ 修复 7：`getDataTimestamps` 加身份校验
8. ✅ 修复 8：`parseXlsx`/`parseCsv` 加身份校验
9. ✅ 修复 9：标记废弃代码
10. 更新文档